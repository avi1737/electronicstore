import React from 'react';
import Header from './auth/components/Header';

const Home = (props) => {
    return(
        <>
        <Header heading = { "Croma" } />
        </>
    )
}

export default Home;